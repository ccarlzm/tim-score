from django.apps import AppConfig

class SchedulingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "compcore.apps.scheduling"
    verbose_name = "Scheduling (Cronograma)"